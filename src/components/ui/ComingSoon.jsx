import { motion } from "motion/react";

const ComingSoon = () => {
  return (
    <motion.main className="w-full h-full justify-center items-center">
      ComingSoon
    </motion.main>
  );
};

export default ComingSoon;
