# Railpack may copy cached node_modules; wipe Vite's cache dir before a clean install.
[phases.install]
cmds = [
  "rm -rf node_modules .vite",
  "npm ci --legacy-peer-deps",
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
